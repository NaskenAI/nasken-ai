# Copy this file to .env.local and fill in the values
# .env.local is gitignored — never commit secrets

# Optional — for the contact form, when you wire it to a real backend
# RESEND_API_KEY=
# CONTACT_FORM_TO_EMAIL=info@nasken.ai

# Optional — analytics
# NEXT_PUBLIC_GA_ID=
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=nasken.ai
